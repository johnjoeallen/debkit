# Full Example

Every configuration section, with inline comments. A few values below reflect a real
host's actually-observed state (RAM, NIS domain/server, `mem_sleep`, the `i2c-dev` gap)
rather than fabricated placeholders — see the comments for which is which.

```yaml
{{#include ../../config.example.yaml}}
```
